# Normalize line endings: store as LF in the repo, check out native on disk.
# This stops the "LF will be replaced by CRLF" warnings on Windows.
* text=auto eol=lf

# Treat binary assets as binary (never normalize / diff them as text)
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.ico binary
